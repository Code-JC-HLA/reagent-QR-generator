<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Lotus</title>

  <!--
    Strict CSP:
      - default-src 'self': only own origin for all resources by default
      - script-src 'self': only local JS (no inline, no third-party)
      - img-src 'self' data:; allow images from same origin + data URIs
      - style-src 'self': only local CSS (no inline CSS)
      - object-src 'none': no <object> or <embed>
      - base-uri 'self': prevent <base> from point elsewhere
      - frame-ancestors 'self' https://*.sharepoint.com: allow embedding only on your origin or on any SharePoint domain
  -->
  <meta http-equiv="Content-Security-Policy"
        content="
          default-src 'self';
          script-src 'self';
          img-src 'self' data:;
          style-src 'self';
          object-src 'none';
          base-uri 'self';
          ">

  <!-- 1. Load QRious from local file -->
  <script src="qrious.min.js"></script>
  <!-- 2. Load your application logic -->
  <script src="app.js"></script>
  <!-- 3. Load external CSS (moved from inline) -->
  <link rel="stylesheet" href="styles.css">
</head>

<body>
  <div class="overlay">
    <h1>Lotus – Lots Used</h1>
    <form id="qr-form" novalidate>
      <!-- Assay Type at the top -->
      <label for="assay">Assay Type:</label>
      <select id="assay">
        <option value="SAB">ABS</option>
        <option value="C1Q">C1q</option>
      </select>

      <!-- Tasklist and Username on one line -->
      <div class="reagent-row">
        <div class="reagent-field">
          <label for="tasklist">Tasklist:</label>
          <input
            type="text"
            id="tasklist"
            maxlength="30"
            placeholder="e.g., C1Q-551"
            required
          />
          <div class="error" id="error-tasklist"></div>
        </div>
        <div class="reagent-field">
          <label for="username">Username:</label>
          <input
            type="text"
            id="username"
            maxlength="30"
            placeholder="Enter your username"
            required
          />
          <div class="error" id="error-username"></div>
        </div>
      </div>

      <div id="reagents"></div>
      <div id="button-bar">
        <button type="button" id="generateBtn">Generate QR Code</button>
        <button type="button" id="printBtn" disabled>Print QR</button>
      </div>
    </form>

    <!-- Hidden until QR is generated -->
    <div id="qr-container">
      <canvas id="qr-canvas"></canvas>
    </div>

    <div id="table-container"></div>
  </div>
</body>
</html>
